{
  "name": "miwallet-passkit-push-relay",
  "description": "Relays PassSource pass-update notifications to WhatPass's Android app via Firebase Cloud Messaging, so PassSource never needs to hold an FCM credential directly.",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "private": true
}
