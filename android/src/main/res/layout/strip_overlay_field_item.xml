<?xml version="1.0" encoding="utf-8"?>
<!-- Used only for primaryFields overlaid on top of a strip/banner image
     (storeCard/coupon/eventTicket) - see PassViewFragment. Text color is always
     white here regardless of the pass's own labelColor/foregroundColor, since it
     must stay legible over an arbitrary photo. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingTop="4dp"
    android:paddingBottom="4dp"
    android:orientation="vertical">

    <TextView
        android:id="@+id/value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-medium"
        android:textColor="@android:color/white"
        android:textSize="32sp"
        tools:text="Value"/>

    <TextView
        android:id="@+id/key"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-light"
        android:textColor="@android:color/white"
        android:textSize="14sp"
        tools:text="Key"/>
</LinearLayout>
